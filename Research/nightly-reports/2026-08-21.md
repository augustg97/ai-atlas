# Morning report — 2026-08-21 (Friday)

**Chain: exit 0 — published and live-verified.** `DATA_V = "20260821-1049"` requested and returned
by `https://augustg97.github.io/ai-atlas/`. Commit `3bfcfce` "Nightly emit 2026-08-21". Vault
resolved at the primary location (`~/Vaults/August's Vault`); the legacy iCloud path is absent,
which is expected. Run 10:47–10:50 plus the live-stamp wait.

Two preflight checks from the 2026-08-20 traps, both clean: `pgrep -f forecast_emit` returned
nothing before the run, and the working tree was empty at start, so tonight's commit carries
generated artefacts only — no repeat of the live-session tree that shipped under a stamp message
yesterday. `Research/staged/forecast/` is **one coherent frame**: all eleven files at 10:49,
`network.json` at `r8-2026-08-20` / date `2026-08-21`.

---

## 1. Calibration — the claims register (§3 of the task)

**No claim has passed its deadline, and none resolved visibly against today's record.**

| claim | by | days out | support | status |
|---|---|---|---|---|
| `cl-state-laws-2026` state AI-law count exceeds 90 | 2026-12-31 | 132 | `R2` | open |
| `cl-sc-2027` superhuman-coder milestone crossed | 2027-12-31 | 497 | `T1` (gap declared) | open |
| `cl-correction-2027` major AI capex correction | 2027-12-31 | 497 | `E3`, `E4` | open |
| `cl-deal-2029` US–CN AI agreement concluded | 2029-12-31 | 1228 | `C3` | open |

The three mis-keys found on 2026-08-20 are gone from the register: `cl-state-laws-2026` now draws
on `R2` "Contested patchwork" instead of a US–CN coordination rung, and `cl-correction-2027` on
`E3`+`E4`. `cl-sc-2027` keeps `T1` with its shortfall declared rather than a plausible key.

**The state-law witness did not move: 8 enactments across 7 states, unchanged since 2026-07-31.**
Today's record does contain a state-legislation item — Nathan Johnson, Democratic nominee for
Texas attorney general, saying on 2026-08-21 that Texas AI statutes fall short and promising
recommendations within 30 days of taking office. That is a campaign commitment, not an enactment,
and the witness correctly ignored it.

**The 109-law tension is now in its third night and still unwired.** The live `R` axis description
reads *"United States states enacted 109 AI laws and 28 data-center statutes in the first half of
2026 from 1,561 bills across 45 states"*. 109 > 90 in H1 alone, against a claim whose own witness
counts 8. The witness says honestly what it is — a lower bound able to confirm and never to refute,
because the wiki tracks policy-notable developments rather than fifty states' legislative output.
Whether the 109 counts the claim's universe is exactly the question a named tracker would settle.
**Recorded again as an open tension, not a resolution.**

---

## 2. Counts

```
events        n 2220 · explicit 91.1% (baseline 91.7) · target 81.6% (baseline 81.6)
review        202 items · 9.099%
grounding     direct 1488 · corpus 457 · total 1945
              direct pages/axis  A 530 · C 375 · D 129 · E 166 · G 76 · K 0 · L 0
                                 P 210 · R 0 · S 232 · T 596
              widening (thin)    K 1.25 · R 1.25 · L 1.25 · G 1.136 · D 1.056
witness       matched 20 · disagreements 4 · gaps 133
statelaw      observed 8 · states 7 · 0 bad
datum         v1-2026-07-31 · max_drift 0.0 · 0 unpositioned · 0 unterritoried
embeddings    98 files / 797 chunks refreshed in 80.8s (2,814 unchanged)
windows 0 bad · cards 0 bad · datekinds 0 bad · eras 0 bad · forecast 0 bad
```

**GATE: PASS (baselines hold).**

```
DRIFT vs 2026-08-20:
  ↓ events.explicit_pct        91.4  → 91.1   ERODED
  ↑ events.target_pct          81.4  → 81.6
  ↑ review.pct                 9.182 → 9.099
  ↑ forecast.grounded_direct   1472  → 1488
RATCHET: review.pct 9.182 → 9.099 · forecast.grounded_direct 1472 → 1488
```

`explicit_pct` is flagged ERODED and the gate passed, correctly: the baseline is the frozen 91.7
with a 1.0pp tolerance (`audit_all.py:270`), so the floor is 90.7 and 91.1 clears it. Worth
watching rather than acting on — **the cumulative fall from baseline is now 0.6pp of the 1.0pp
tolerance, and this metric has never ratcheted.** Two others improved tonight and tightened, so
their slack is spent; `explicit_pct` is the one drawing down. 40 new events arrived (2180 → 2220)
and diluted it.

**K, L and R still have zero direct grounding pages** and carry the maximum 1.25 widening. G, added
yesterday, holds at 76 pages and 1.136.

---

## 3. Forecast update

```
fresh 20 · incidents 20 (0 reports collapsed) · applied 4 · residue 16 · contested 0
recovered late 14 (event dates 2026-08-14 … 2026-08-20) · seen ledger 351 → 371
residue bank 250 → 266 · evidence log 97 → 101
schema additions 0 — not a Monday; the review last added anything 2026-08-10
```

**No SCHEMA ADDITIONS block this run.**

### The four, with their drivers

| rule | tier | ×impact | driver |
|---|---|---|---|
| `ev-export-retaliation` | **notable** | **×0.00800** (k=0.00, spread 1.00, 1 report / 1 src) | House ranking members asked Commerce Secretary Lutnick to explain why BIS eased AI export controls for the UAE, and whether it was connected to Trump family business dealings |
| `ev-benchmark-progress` | minor | ×0.00172 (k=1.00, spread 1.00, 1/1) | Mistral released Agentic Search on 2026-08-20; FinanceBench correctness 26.7% → 86% |
| `ev-compute-buildout` | minor | ×0.00058 (k=4.28, spread 1.00, 1/1) | Micron said on 2026-08-20 it will invest $10bn over a decade in a Boise memory research lab |
| `ev-capital-commitment` | minor | ×0.00045 (k=15.54, spread 1.00, 1/1) | Poolside AI struck a $6bn non-exclusive Nvidia licensing deal plus a $1bn investment at a $12bn pre-money valuation |

**`ev-export-retaliation` fired for the first time in the machine's life tonight** — 101
applications in, zero prior firings, so `repeat_k = 0.00` and the novelty multiplier is at its
maximum. Combined with the `notable` tier that makes it **tied for the largest single evidence
application ever recorded here** (0.00800, against a median of 0.00057 — 14× the median). It is
also the one that went wrong. §4.

### Day-over-day marginal movement (08-20 → 08-21, pp)

```
C   C1 +0.7996  C2 -0.0928  C3 -0.5475  C4 -0.0696  C5 -0.0283  C6 -0.0307  C7 -0.0260  C8 -0.0047
S   S1 -0.2372  S2 -0.1415  S3 +0.6653  S4 -0.1878  S5 -0.0988
T   T1 -0.0172  T2 +0.1155  T3 +0.1261  T4 -0.2037  T5 -0.0206
E   E1 +0.0533  E2 -0.0054  E3 -0.0398  E4 -0.0057  E5 -0.0024
D   D1 +0.0088  D2 -0.0517  D3 +0.0046  D4 +0.0383       (grounding only — no evidence applied)
G   G1 -0.0057  G2 -0.0274  G3 +0.0053  G4 +0.0053  G5 +0.0138  G6 +0.0088   (grounding only)
K · R · P · L   — no movement at all
```

Mainline unchanged: **T2 · K1 · A2 · C1 · R4 · D2 · S1 · P3 · E3 · L4 · G4**, p 7.3532e-06 →
7.4936e-06 (+1.9%).

Tonight's board moved about ten times as far as a normal night, and one application accounts for
nearly all of it. **C1 +0.80pp and S3 +0.67pp are the largest evidence-driven position moves since
2026-08-04**, when the only other 0.00800 application in the machine's life — Illinois HB 5511 —
moved `C4` +2.01pp. Bigger swings appear on 08-13, 08-16 and 08-17, but those are prior changes and
the r5 position-space rebuild, not evidence:

```
largest position move per night, all history (evidence nights, rebuild nights marked)
  08-04  C4  +2.0080     08-13  P1  +11.6688  rebuild    08-19  E1  +0.0803
  08-08  C4  +1.0162     08-16  T2  +13.0569  rebuild    08-20  E3  +0.6142
  08-11  S3  +0.3490     08-17  E2  -24.4160  rebuild    08-21  C1  +0.7996
```

---

## 4. The rules were never re-keyed off r5 — and tonight's biggest application went through the gap

Yesterday's r8c re-read the **claims register and the crisis board** against the live names and
fixed four keys. It did not re-read the **evidence rules**, which are the surface that writes.
Audited tonight, rule nudge targets against the position names as they stood immediately before r5
(`69ce45f~1`):

```
nudge targets              75   across 31 rules
  letter existed pre-r5, NAME CHANGED    60   (26 rules)
  letter is post-r5 (written after)      15   (the 5 G rules added yesterday)
  name unchanged                          0
```

**Every rule written before 2026-08-17 points at a coordinate whose label r5 changed.** Most of
those are cosmetic — `T2` "fast (2029-31)" → "2029 to 2031" is the same band, `A1` "fails
undetected" → "Control lost undetected" is the same cell, `E1` "boom sustained" → "Boom holds".
Those are not defects and should not be counted as any. What matters is the subset where **the
meaning moved**, and the clearest of them is what fired tonight.

### `ev-export-retaliation` — the item, and where it landed

The rule matched on `"export control*"` inside *National Security & Geopolitics* and applied
`C1 +2, S3 +2, C3 −1`. Read against the live r8 names:

- **`S3` +0.0096 raw, +0.6653pp — the wrong position by a letter that changed meaning.**
  Pre-r5, `S3` was *"constrained (controls + energy)"* and export controls were inside its
  definition; the key was correct when it was written. r5 split that cell in two: **`S3` is now
  "Power and siting bind"** — *"grid connection and local permission set how fast new capacity
  comes online"* — and **`S4` is "Trade policy binds"** — *"licence volume between the principals
  sets who can train at frontier scale, and the licence is rewritten quarterly."* An export-licence
  reclassification is `S4` by its own subject matter. The board's largest move of the night went
  onto grid permitting, and `S4` **fell 0.19pp** in the normalisation.
- **`C1` +0.7996pp on an item about controls being loosened.** `C1` "Unilateral controls" means
  each capital writes and enforces its own rules for the other's access. The event underneath is
  BIS reclassifying the UAE in July 2026 from Country Groups D:3/D:4 to A:5 — a **relaxation**, and
  toward a third country, not China. The visible surface is an oversight letter criticising it.
  The rule raised "unilateral controls" by 0.8pp on the record of a control being eased.
- `C3` −0.5475pp is the smaller of the pair and roughly defensible: `C3` "Declaratory accord" is
  close enough to its pre-r5 sense ("US-CN transparency deal") that pushing it down on friction news
  is not obviously wrong.

The direction defect recorded on 2026-08-15 and 08-16 was diagnosed then as *a rule matches on
event TYPE, which says nothing about DIRECTION*. **Tonight adds a second mechanism to the same
symptom, and it is a different one: the rule read the direction correctly for the registry it was
written against, and the registry moved underneath it.** A rule can be wrong because it cannot see
what the finding says, or because the coordinate it names stopped meaning what it meant. Only the
first is fixed by a `text_none` guard.

### The other rules whose target moved materially

Not fired tonight, but standing in the library and live the moment their patterns match:

| rule | target | was, pre-r5 | is, now | reading |
|---|---|---|---|---|
| `ev-compute-constraint` | `S3` +2 | constrained (controls + energy) | Power and siting bind | reaches only the energy half; trade constraint can no longer be expressed |
| `ev-distillation-attack` | `S3` +1 | " | " | a weights-exfiltration item pushed onto grid permitting |
| `ev-enterprise-adoption` | `P2` +1 | adoption/acquiescence | **Stable disapproval** | inverted — adoption news now raises public disapproval |
| `ev-inference-tax-proposal` | `P1` +1 | populist backlash | **Acquiescence through use** | inverted |
| `ev-moratorium-movement` | `P1` +1 | populist backlash | **Acquiescence through use** | inverted |
| `ev-moratorium-movement` | `C5` +2 | moratorium / shutdown | **Verified limit holds** | a domestic moratorium movement now raises US–CN verified coordination |
| `ev-standards-published` | `C5` −1 | moratorium / shutdown | **Verified limit holds** | inverted — published standards now *reduce* the chance a verified limit holds |
| `ev-unemployment-spike` | `E4` +2 | displacement-led demand crisis | **Capital expenditure cut** | r5 inserted `E4`; the demand-crisis cell is now `E5`. Same insertion that mis-keyed `cl-correction-2027` |
| `ev-unemployment-spike`, `ev-agentic-deployment` | `D3` −1 | slow absorption | **Broad absorption** | inverted — pushes down on broad absorption where it pushed down on slow |
| `ev-state-law-enacted`, `ev-state-bill-activity`, `ev-regulatory-implementation`, `ev-enforcement-action` | `C4` +2 | fragmented blocs | **Domain-confined limit** | domestic legislation writing into the US–CN coordination ladder. **This is the exact mis-key fixed in `cl-state-laws-2026` yesterday, still live in the four rules that feed it** |
| `ev-federal-preemption-push`, `ev-natsec-posture`, `ev-sabotage-ops`, `ev-distillation-attack`, `ev-frontier-release-gov-coordinated` | `C2` +2 | US securitization | **Licensed channel** | a domestic posture key now writes to a US–CN export-licensing rung |
| `ev-safety-research` | `A3` +2 | tractable with effort | **Catch buys pause** | the 08-15 finding, restated: `A3` now means a detected failure moves a release schedule ≥10 months. A published safety result is evidence for the old `A3`, whose r8 homes are `A4`/`A5` |

### How much of the record has gone through it

```
applications since r5 (2026-08-17)      22
position-writes                         47
  onto a letter r5 renamed              44
  onto a letter r5 renamed MATERIALLY   10   (S3 ×4, A3 ×3, C2 ×2, C4 ×1)
  plus E3 ×11, where the capex half of the old cell moved to E4 and the rule
       does not reach it
```

The 44 is the honest denominator and not the finding; the 10 are. `E1`/`E3` carry `ev-capital-
commitment`, the most-fired rule in the set, and its counterweight now covers half of what it was
written to cover.

### The guard built yesterday cannot see this

`forecast_emit.unkeyed_cards()` (`forecast_emit.py:530`) iterates `CRISES` and `CLAIMS`. It checks
a hand-declared axis per card and asks `capability_path` whether the position points the way the
card asks — a good check, with a planted positive control, and it passes tonight. `EVIDENCE_RULES`
is not in it. **The check was written for the surface where the defect was found rather than for
the mechanism that produces it**, which is the same shape as the 08-20 note that a gate written
against the examples in front of you fits those examples.

Nothing was re-keyed tonight. Choosing which position each rule should target is a modelling
decision — `S4` for the trade half of `ev-export-retaliation` is clear, `C4` → `R2` for the four
domestic-legislation rules is nearly so, `A3` → `A4`/`A5` is a judgement about what a published
safety result is evidence *for* — and that is August's call, not a nightly's. **Recorded, not
acted on.**

---

## 5. The other three applications

- **`ev-benchmark-progress`** → `T2 +1, T3 +1, T4 −1`. The `T` band names are pure renames, so the
  key holds. Direction is right for a capability gain. One caveat the rule cannot express: the
  digest states plainly that the Mistral figures are **vendor-run** on Mistral's own stack with
  default chunking and ranking. Nothing in the rule discounts a vendor benchmark against an
  independent one, and `T4` fell 0.20pp on it.
- **`ev-compute-buildout`** → `S2 +2, S3 −1`. `S2` "Capital binds, capacity diversifies" is a fair
  home for a $10bn memory research lab. The `S3 −1` counterweight reaches only the power-and-siting
  half of what it was written against.
- **`ev-capital-commitment`** → `E1 +2, E3 −1`. Poolside/Nvidia is capital committing, `E1` "Boom
  holds" rose 0.05pp, direction right. Its counterweight `ev-big-correction` has still **never
  fired in 101 applications** — the asymmetry recorded on 2026-08-20 is unchanged: the optimistic
  rule matches publication-genre words that appear whenever the topic does, its counterweight needs
  the adverse word literally.

**13 of 31 rules have never fired.** Among them: four of the five benefit rules added yesterday
(`ev-device-authorisation` is the one that has fired, once); **both structural-tier rules**
(`ev-us-cn-agreement`, `ev-autonomous-rd-demonstrated`); and **every major- or notable-tier rule on
E or D** — `ev-big-correction`, `ev-unemployment-spike`, `ev-inference-tax-proposal`, all at zero.
The three E/D rules that *have* fired are all minor tier, and `ev-capital-commitment` alone
accounts for 32 of the 101 applications. The heavy instruments on the economy and labour axes have
never once been picked up.

---

## 6. Residue

16 unmatched, banked with their event dates (bank 250 → 266). Five carry today's date:

```
Federal AI Policy & Agency Action     2
U.S. State AI Legislation             2
AI Industry & Markets                 1
```

Those five, verbatim from the bank: the civil-society letter urging the FTC to investigate AI firms
buying and destroying books for training data; the NIST SP 1353 quick-start draft on AI and CSF
2.0; the Texas AG nominee; the Hochul data-center moratorium update; and Meta buying model access
through Azure. The remaining 11 carry earlier event dates, back to 2026-08-14.

Two of the five are a coverage map rather than a miss. **No rule takes public response as its
subject** — the Hochul item is a governor calibrating against constituent opposition, and the only
`P`-touching rules are `ev-enterprise-adoption` and `ev-inference-tax-proposal`, both of which
reach `P` as a side effect of something else and both of which are inverted (§4). **And no rule
takes a federal agency's own guidance as its subject** — the NIST draft is `R`, which no rule can
name at all.

No cluster reached `RESIDUE_CLUSTER_MIN` (12 in 7 days), so no schema addition was due — and it is
not a Monday in any case.

---

## 7. What tomorrow needs

1. **Re-key the evidence rules off r8, or add them to the card check.** §4. The 26 pre-r5 rules
   are the write surface and nothing has re-read them since the position space was rebuilt on
   2026-08-17. The `S3`/`S4` split and the four `C4` domestic-legislation rules are the two with
   the clearest answers.
2. **Extend `unkeyed_cards()` to `EVIDENCE_RULES`** — a declared axis per rule, the same shape as
   `CARD_AXIS`, so the next rebuild cannot re-key the nudges quietly either.
3. Carried from 2026-08-20, none of it addressed tonight: **rules or pages for K, L and R** (still
   zero grounding, still unnamed by any rule, 1.25 widening); **`text_none` guards on the
   direction-blind pairs**; **`explosive-takeoff`'s registry prose and its own arithmetic a year
   apart**; **a lock file on the nightly chain**.
4. `marginals.history` still holds **28 entries over 22 distinct dates** — 08-17 stored four times,
   08-13/16/18 twice each. A row-count window over it weights those days 2–4×. Unchanged since
   2026-08-19.
5. Watch `events.explicit_pct`: 0.6pp of a 1.0pp tolerance spent, one-way, never ratcheted.
