# Morning report — 2026-08-22 (Saturday)

**Chain: exit 0 — published and live-verified.** `DATA_V = "20260822-1049"` requested and returned
by `https://augustg97.github.io/ai-atlas/`. Commit `d4de4e8` "Nightly emit 2026-08-22". Vault
resolved at the primary location (`~/Vaults/August's Vault`). Run 10:47–10:50 plus the live-stamp
wait; well inside the 15-minute budget.

Both preflight checks from the 2026-08-20 traps clean: `pgrep -f forecast_emit` returned nothing
before the run, and the working tree was empty at start, so tonight's commit carries generated
artefacts only. `Research/staged/forecast/` is **one coherent frame** — all eleven files at 10:49,
`network.json` at `r8-2026-08-20` / date `2026-08-22`.

Saturday, so no schema review. **SCHEMA ADDITIONS: 0.**

---

## 1. Calibration — the claims register (§3 of the task)

**No claim has passed its deadline, and none resolved visibly against today's record.**

| claim | by | days out | support | status |
|---|---|---|---|---|
| `cl-state-laws-2026` state AI-law count exceeds 90 | 2026-12-31 | 131 | `R2` | open |
| `cl-sc-2027` superhuman-coder milestone crossed | 2027-12-31 | 496 | `T1` (gap declared) | open |
| `cl-correction-2027` major AI capex correction | 2027-12-31 | 496 | `E3`, `E4` | open |
| `cl-deal-2029` US–CN AI agreement concluded | 2029-12-31 | 1227 | `C3` | open |

**The state-law witness did not move: 8 enactments across 7 states, unchanged since 2026-07-31.**
Tonight's record does carry a state-legislation item — OpenAI urging California on 2026-08-21 to
*strengthen* SB 53 — but that is advocacy on a bill already law, not an enactment, and it went to
residue rather than to the witness. Correct.

**The 109-law tension is now in its fourth night and still unwired.** The live `R` axis description
reads *"United States states enacted 109 AI laws and 28 data-center statutes in the first half of
2026 from 1,561 bills across 45 states"*. 109 > 90 in H1 alone, against a claim whose own witness
counts 8. The witness states honestly what it is — a lower bound that can confirm and never refute.

**New tonight, and it sharpens the tension: `R` has received zero evidence applications in its
entire life.** So has `K` and so has `L`. `cl-state-laws-2026` is keyed to `R2`, and the axis
carrying its support has never once been moved by the nightly evidence pass — its position is
whatever the seed prior and the thin-axis widener (`R` ×1.25) leave it at. The claim is not merely
unresolved; the surface it is scored against is not listening.

---

## 2. Counts

```
events        n 2238 · explicit 91.2% (baseline 91.7) · target 81.5% (baseline 81.6)
emit          entities 949 · people 294 · arcs 13178 · series 525 · wikitime_ops 715
review        202 items · 9.026%
grounding     direct 1495 · corpus 458 · total 1953
              direct pages/axis  A 532 · C 378 · D 129 · E 166 · G 77 · K 0 · L 0
                                 P 211 · R 0 · S 237 · T 599
              widening (thin)    K 1.25 · R 1.25 · L 1.25 · G 1.134 · D 1.056
witness       matched 20 · disagreements 4 · gaps 133 · unmatched_wiki 48
statelaw      observed 8 · states 7 · 0 bad
datum         v1-2026-07-31 · max_drift 0.0 · 0 unpositioned · 0 unterritoried
embeddings    50 files / 420 chunks refreshed in 48.0s (2,877 unchanged)
windows 0 bad · cards 0 bad · datekinds 0 bad · eras 0 bad · forecast 0 bad
```

**GATE: PASS (baselines hold).**

```
DRIFT vs 2026-08-21:
  ↑ events.explicit_pct        91.1  → 91.2
  ↓ events.target_pct          81.6  → 81.5   ERODED
  ↑ review.pct                 9.099 → 9.026
  ↑ forecast.grounded_direct   1488  → 1495
RATCHET: review.pct 9.099 → 9.026 · forecast.grounded_direct 1488 → 1495
```

`target_pct` is flagged ERODED and the gate passed, correctly: the baseline is the frozen 81.6 with
a 1.0pp tolerance (`audit_all.py:273`), so the floor is 80.6 and 81.5 clears it easily — 0.1pp of
1.0pp spent, on the first night this metric has dipped below its baseline at all.

**Yesterday's watch item recovered.** `explicit_pct` went 91.4 → 91.1 → **91.2**; the cumulative
fall from baseline is now 0.5pp of the 1.0pp tolerance, down from 0.6. 18 new events arrived
(2220 → 2238), a fifth of yesterday's 40, so there was less dilution to absorb.

---

## 3. Forecast update

```
fresh 18 · incidents 18 · applied 6 · residue 12 · collapsed 0 · contested 0
recovered_late 18 (event dates 2026-08-17 … 2026-08-21) · seen ledger 389
```

**Mainline unchanged** — `T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4`, p 7.4936e-06 → 7.4268e-06 (−0.9%).
`G4` held for a second night; on the axis marginal `G2` "Rich systems absorb it" leads at 0.259
against `G4`'s 0.152, and `G2` was the runner-up swap on 2026-08-20, so the G coordinate remains
the least-determined in the model. Tonight it moved on grounding only — no rule wrote to G.

### Deltas, ranked, with drivers

| position | | move | driver |
|---|---|---|---|
| `T.T3` | 2032 to 2036 | **+0.3105pp** | `ev-frontier-release` ×0.00270 — DeepSeek released `deepseek-v4-flash-vision-exp` 2026-08-21, an experimental multimodal version of V4 Flash (Bloomberg + aidisruption, 2 src) |
| `T.T4` | 2037 to 2050 | **−0.2120pp** | same application (nudge `T3 +2 / T4 −1`) |
| `C.C4` | Domain-confined limit | **+0.1949pp** | `ev-enforcement-action` ×0.00079 ×2 — DOJ/TikTok/ByteDance $400m COPPA settlement (2026-08-21); Judge Chhabria threw out seven economic espionage counts against a former Google engineer (2026-08-20) |
| `C.C1` | Unilateral controls | **−0.1407pp** | same two applications (nudge `C4 +2 / C1 −1`) |
| `E.E1` | Boom holds | **+0.1000pp** | `ev-capital-commitment` ×0.00026/0.00032/0.00026 — Anthropic IPO preparations; Nvidia minority investment in Cloverleaf Infrastructure; Starcloud $250m Series A extension |
| `E.E3` | Valuation correction | **−0.0747pp** | same three applications (nudge `E1 +2 / E3 −1`) |
| `T.T2` | 2029 to 2031 | −0.0592pp | normalisation |
| `T.T5` | Method asymptote | −0.0214pp | normalisation |
| `C.C2` | Licensed channel | −0.0205pp | normalisation |
| `G.G2` | Rich systems absorb it | +0.0183pp | **grounding only** — widener moved G 1.136 → 1.134 |

Remaining moves are all below 0.02pp and are normalisation or grounding. **Evidence touched three
axes of eleven** — T, C, E. A, K, D, S, P, R, L took nothing; G moved on grounding alone.

---

## 4. Findings

### 4.1 The 2026-08-21 write-surface defect, demonstrated on the night's largest C move

Yesterday's finding was that all 60 pre-r5 rule nudge targets still point at letters r5 renamed.
Tonight it produced a clean example.

`ev-enforcement-action` fired twice and put **+0.1949pp on `C4`**. Under r5, `C` is *"Coordination
between principal states"* — how much of frontier development a **US–China agreement** constrains —
and `C4` "Domain-confined limit" means *"Both principals accept a real obligation covering one
capability domain"*, cited to the IISS US–China military-AI dialogue and the State Department
declaration on responsible military use of AI.

The two drivers are a US consumer-privacy settlement and a US criminal-docket ruling.

The rule's own comment still describes the axis it was written for: *"courts and regulators acting
without a federal statute is the fragmented road; a preemption push is the argument against it"* —
a US-domestic fragmentation ladder. Its citations are `analysis/eu-vs-us-ai-regulation` and
`legislation/eo-trump-december-2025-state-preemption`. **The rule, its comment, and its citations
all describe one axis; the letter it writes to now names another.**

**And it is not one rule.** `C` has taken 44 position-writes across its life, from eight rules —
`ev-enforcement-action` (20), `ev-regulatory-implementation` (6), `ev-state-law-enacted` (4),
`ev-federal-preemption-push` (4), `ev-standards-published` (4), `ev-state-bill-activity` (2),
`ev-natsec-posture` (2), `ev-export-retaliation` (2). Every one is US-domestic or export-control in
subject. `ev-us-cn-agreement` — the single rule whose subject actually *is* this axis — has never
fired. **The US–China coordination axis has received 44 evidence writes and not one of them was
about a US–China agreement.**

### 4.2 The T rule cuts against the wrong null

`ev-frontier-release` nudges `T3 +2 / T4 −1`, and its comment states the intent outright: an
ordinary release *"supports the gradual road and cuts against the no-SC-in-window null."*

Under r5 the no-arrival cell is **`T5` "Method asymptote"**. `T4` is "2037 to 2050" — a late but
real arrival date. So tonight the rule spent its entire negative nudge, **−0.2120pp, arguing
against a late arrival**, while `T5` — the cell it means to cut against — fell 0.0214pp, a tenth as
much and only as normalisation. Same class as §4.1, and the same class as the crisis-board mis-key
found on 2026-08-18: a nudge keyed to a letter whose meaning r5 moved.

Separately on this application: the driver is an **experimental multimodal *flash* variant** — a
cost-and-latency line, not a research-loop capability push — and it carried the night's largest
application at ×0.00270, roughly **ten times** either enforcement application. It got that size from
`spread=1.00` and `repeat_k=0.578` (novel, unrepeated), not from any bearing on the research rung.
Novelty scaling is doing the work that relevance should.

### 4.3 `fine` is not `fined` — the night's largest penalty went to residue

The single largest enforcement penalty in tonight's record — the Autoriteit Persoonsgegevens
imposing **€824,990,000 on Uber**, filed under "AI Litigation, Liability & Enforcement" — matched
**none** of `ev-enforcement-action`'s thirteen terms and was banked as residue.

The rule lists `"fined"` (past-tense verb) and `"penalt*"`. The item says *"imposed a fine of"*.
`fine` as a noun is in no term.

Meanwhile a court **throwing out** seven counts did match, and carried the identical `C4 +2 / C1 −1`
vector as a settlement the government won. **On one night, in one section, the rule caught
enforcement being defeated and missed enforcement at €825 million.**

This is the 2026-08-20 asymmetry in miniature: the terms that fire are the ones present whenever the
topic comes up, and an adverse fact has to appear in exactly the inflection listed. The fix is the
same shape as r2's `invest*` repair — enumerate rather than stem, since `fin*` reaches "finance" and
"final": add `"fine"`, `"fines"`, `"fining"` alongside `"fined"`.

### 4.4 `E1` has never fallen on evidence

Date-ordered, one row per date, `E1` "Boom holds" across 23 nights: **17 up, 2 flat, 3 down** — and
all three down moves are registry rebuilds, not evidence (−0.0243 on 08-01, −0.0269 on 08-13,
−0.0434 at the r5 rebuild on 08-17).

`E` has taken **70 position-writes in its life, all 70 from `ev-capital-commitment`** — 35
applications, a third of the 107 in the whole log. Its designed counterweight `ev-big-correction`
has fired **zero** times. Tonight added three more one-way applications.

The axis has one input, and that input can only push one way.

### 4.5 Coverage of the rule library

**13 of 31 rules have never fired**: `ev-autonomous-rd-demonstrated`, `ev-benefit-delivery`,
`ev-benefit-unmeasured`, `ev-big-correction`, `ev-clinical-readout`, `ev-clinical-shortfall`,
`ev-distillation-attack`, `ev-frontier-release-gov-coordinated`, `ev-inference-tax-proposal`,
`ev-moratorium-movement`, `ev-sabotage-ops`, `ev-unemployment-spike`, `ev-us-cn-agreement`.

Position-writes by axis, all time: `E` 70 · `A` 54 · `C` 44 · `S` 27 · `T` 18 · `D` 8 · `G` 3 ·
`P` 1 · **`K` 0 · `L` 0 · `R` 0**.

Two rules supply 116 of 225 position-writes (`ev-capital-commitment` 70, `ev-safety-research` 46). Three axes have never been written to at all.

### 4.6 `marginals.history` still not deduplicated or date-ordered

29 rows across 23 distinct dates. `2026-08-17` is stored **4×**; `08-13`, `08-16` and `08-18` twice
each. Found 2026-08-19, unfixed. Any window taken by row count over this weights those four dates
2–4×; the E1 series in §4.4 had to be rebuilt as last-row-per-date before it could be read at all.

---

## 5. What was not done

No task was filed into `Wiki/_meta/queue/`. The gate passed and none of tonight's findings is a
**wiki data defect** — §4.1 through §4.5 are all defects in `Research/timelines/axes.py`, and §4.6
is in `weights.json`. The queue is the write path for wiki content; these belong in a build round.

No fix was applied. Every finding above is a rule-library change that moves published probabilities,
and the nightly task's remit is to run the chain and report.
