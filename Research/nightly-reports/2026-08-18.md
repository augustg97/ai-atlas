# Morning report — 2026-08-18 (Tuesday)

**Status: PUBLISHED + VERIFIED** · `DATA_V = 20260818-1054` · gate PASS (baselines hold) · exit 0
First attempt, no retry, no `SKIP_AUDIT`, no gate override. The chain's own live check passed
(`live: DATA_V = "20260818-1054" / want: DATA_V = "20260818-1054"`, `nightly OK 2026-08-18-1055`),
and the live page was then rendered and read in this session.

**Morning 19 — `r6` landed overnight and closed the two edges it set out to close. The panels that
name the model's own questions did not move with it, and now read the wrong cells.** The crisis
board and the claims register are hardcoded lists in `forecast_emit.py`, written 2026-07-31 against
the r4 space of 7 axes and 26 positions. `r5` (2026-08-17) and `r6` (2026-08-18) rebuilt that space
to 9 axes and 49 positions and **redefined the letters they reference**. Nothing remapped them.

Live, on the page right now:

> **No superintelligence this window — 18%**

That figure is `P(T4)`. Under r4, `T4` was *"continuous-normal (no SC in window)"* and the label was
exact. Under r6, **`T4` is "2037 to 2050"** — an arrival date, inside a window that runs to 2100. The
cell that means the rung is never crossed is now **`T5` "Method asymptote" (12%)**, and the crisis
board does not read it. The board reports the probability of a *late arrival* under the words *no
arrival*.

Same failure, five more times, verified against the live page (`network.version` = `r6-2026-08-18`):

| the board says | it reads | r4 meaning of that cell | painted |
|---|---|---|---|
| No superintelligence this window | `T4` = **2037 to 2050** | *continuous-normal (no SC in window)* | 18% |
| Explosive tempo (SC by 2028) | `T1` = **2027 to 2028** | *explosive (SC 2027-28)* | 10% |
| US-China transparency deal by 2032 | `C3` = **Declaratory accord** | *US-CN transparency deal* | 14% |
| The bubble deflates hard | `E3` = **Valuation correction** | *deflates hard (capex-led)* | 32% |
| Alignment fails undetected | `A1` = **Control lost undetected** | *fails undetected* | 9% |
| Superhuman AI researcher by 2035 | band, rung 4.0 @ 2035 | — | 68% |

Only the last two survive the rebuild. `A1` happens to have kept its meaning; the band question is
indexed to a **rung on the ladder**, not to a letter, and is the only item in the panel that a
position-space rebuild cannot break. That is the design lesson: **a panel keyed to a letter inherits
whatever the letter later means; a panel keyed to a measured quantity does not.**

The three that are wrong are wrong in three different ways, and only one is an inversion:

- **`no-sc-window` is inverted** — it names the absence and reads an arrival.
- **`explosive-takeoff` names a subject that is no longer T's.** `r5` added **K, takeoff shape,
  precisely to unweld tempo from arrival date**. "Explosive tempo" is now `K1` *"rungs inside one
  year"*, which stands at **51.5%**. The board prints 10% beside the word "tempo".
- **`deal-window` and `hard-deflate` undercount** by reading one cell of a set that r6 split.
  A concluded US-CN agreement is now true in `C2`–`C7`, not `C3` alone. A hard capex-led deflation
  is now `E3` *plus* `E4` *"Capital expenditure cut"* (0.322 + 0.191 = **0.513** against the 32%
  shown).

---

## 1. Calibration — the claims register (§3 of the task)

**No claim passed its deadline tonight, and none resolved.** All four remain `open`; the nearest
deadline is `cl-state-laws-2026` at 2026-12-31, 135 days out. The state-law witness ran and reported
**8 enactments across 7 distinct states** against a threshold of 90 — a **lower bound** that, as the
witness itself states in the data, can confirm the claim but can never refute it, because the wiki
tracks policy-notable developments rather than fifty states' full legislative output.

But the register has the same defect as the crisis board, and here it reaches the one claim with
live measuring machinery attached:

| claim | `axis_support` | what that cell means under r6 | reading |
|---|---|---|---|
| `cl-state-laws-2026` "state AI-law count exceeds 90" | `C4` | **Domain-confined limit** — a *US–China* arrangement | **wrong axis.** The subject is the US state patchwork, which `r5` moved to **R2 "Contested patchwork"** |
| `cl-correction-2027` "major AI capex correction" | `E2`, `E3` | Margin squeeze + Valuation correction | **excludes `E4` "Capital expenditure cut"** — the cell that names the claim |
| `cl-deal-2029` "US-CN AI agreement concluded" | `C3` | Declaratory accord | narrowed to one of six cells in which an agreement exists |
| `cl-sc-2027` "superhuman-coder milestone crossed" | `T1` | 2027 to 2028 | date holds, but T is now scored at the **research** rung (4.0); the coder rung is 3.0, and `r5` added K to separate them |

`cl-state-laws-2026` is the sharp one. `witness_statelaw` counts real enacted state laws, ties them
to the claim, and the claim draws its support from a position on the US–China coordination axis.
**The one place the model touches ground is wired to the wrong axis.** No scoring machinery has
landed yet, so nothing was mis-scored — but the wiring is in place for it to be, and it is the
wiring the scoring round will build on.

---

## 2. Counts

| stage | prev (08-17 10:52) | **08-18 10:54** | Δ |
|---|---|---|---|
| trunk entities | 927 | **930** | +3 |
| people | 291 | **291** | 0 |
| events | 2,131 | **2,155** | +24 |
| arcs | 12,838 | **12,868** | +30 |
| series rows | 493 | **497** | +4 |
| datum positions | 1,842 | **1,842** | `max_drift 0.0` · `v1-2026-07-31` |
| explicit date-kind | 91.4% | **91.3%** | **ERODED** (target 81.6%) |
| needs-review | 9.855% | **9.791%** | improved (211 of 2,155) |
| grounded direct | 1,431 | **1,434** | +3 (corpus 486 · total 1,920) |
| witness-epoch | 20 matched · 4 disagree | **20 · 4** | 133 frontier gaps |

Gate checks all clean: windows 0 bad, cards 0 bad, date-kinds 0 bad, eras 0 bad, unpositioned
features 0, forecast 0 bad, statelaw 0 bad.

**`explicit_pct` 91.4 → 91.3 is flagged ERODED and the gate still passed** — it is inside the
recorded tolerance, and the drift block exists to print exactly this rather than let it decay
silently. It is 24 new events diluting the rate, not a regression in the parser; the ratchet
tightened `target_pct`, `review.pct` and `grounded_direct` in the same run.

One caveat on that comparison: the drift block prints **"DRIFT vs 2026-08-18"** because `r6` was
committed at 23:00 last night and a run at 00:20 this morning overwrote `last_run`. I checked the
stored values against the 08-17 nightly commit (`dcf6a8b`) — **they are identical** (91.4 / 81.5 /
9.855 / 1431), so no day-over-day information was lost. The label is honest about its date; it is
just not the date a reader assumes.

---

## 3. Forecast update

```
fresh 24 · incidents 22 (2 collapsed) · applied 7 · residue 15
recovered late 17 (event dates 2026-08-04 … 2026-08-17) · seen ledger 306
schema additions 0 (not a Monday — the review runs Mondays; last fired 2026-08-10)
```

**No SCHEMA ADDITIONS block this run.**

**The mainline did not move.** `T2·K1·A2·C1·R4·D2·S1·P3·E3`, identical to 2026-08-17; joint
probability 0.00012731 → 0.00012729, a relative change of −0.02%. Seven `minor` applications, all
with `requested == applied` (no clipping).

### The seven, with their drivers

| rule | ×mag | vector | driver |
|---|---|---|---|
| `ev-capital-commitment` | 0.00023 | `E1 +0.00030` `E3 −0.00015` | Groq raised $350M at $3.5B, led by Disruptive, announced 2026-08-17 |
| `ev-capital-commitment` | 0.00023 | *identical* | Alphabet/Amazon/Meta/Microsoft carry $1.52T purchase commitments, $904B leases not yet started |
| `ev-capital-commitment` | 0.00023 | *identical* | OpenAI executive departures — CFO at the 2026-08-14 investor meeting |
| `ev-capital-commitment` | 0.00023 | *identical* | Anthropic annualized run rate passed $65B end of July, public 2026-08-17 |
| `ev-federal-preemption-push` | 0.00259 | `C2 +0.00259` `C4 −0.00259` | American Bankers Association 21-page submission to House Financial Services Committee Democrats, 2026-08-14 (2 reports) |
| `ev-compute-buildout` | 0.00054 | `S2 +0.00073` `S3 −0.00036` | OpenAI 20-year lease, PORTS-Pike campus, southern Ohio, 8 IT-GW / 10 GW |
| `ev-natsec-posture` | 0.00300 | `C2 +0.00400` `C3 −0.00200` | USCC reported 2026-08-18 that China is commercialising and monetising data as a state asset |

### Day-over-day marginal movement (08-17 nightly → 08-18 nightly, pp)

```
C:  C2 +0.620  C3 −0.228  C4 −0.289  C1 −0.065  C5 −0.012  C6 −0.013  C7 −0.011
E:  E1 +0.102  E3 −0.038  E5 −0.027  E2 −0.019  E4 −0.018
S:  S2 +0.065  S3 −0.045  S1 −0.009  S4 −0.007
D:  D2 +0.019  D4 −0.014
T, K, A, R, P:  no movement
```

**The largest move of the night, C2 +0.620pp, is domestic US evidence landing on the US–China axis.**
`ev-federal-preemption-push` matches on section *Federal AI Policy & Agency Action* and nudges
`C.C2 / C.C4`. Tonight it fired on a bankers' association letter to a US House committee and moved
*coordination between principal states*. Meanwhile **`R3` is literally named "Federal preemption"**
and received nothing, because — see §4 — no rule in the library can reach R at all.

**`E1` "Boom holds" rose again, +0.102pp.** Across the whole history, every evidence-scale move of
E1 (|Δ| < 0.5pp) has been **upward — 13 of 13**; the only three declines are −2.4, −2.7 and −4.3pp
and land exactly on the three rebase nights (08-01, 08-13, 08-17). This is the G13/G16 finding
still standing after r6: **`ev-capital-commitment` carries one fixed vector and fires on event
type, which says nothing about direction.** Tonight it fired four times with the identical vector,
and one of those four was *OpenAI executive departures* — a governance-instability story pushed
toward "Boom holds". Driver strings are still cut at 300 chars, mid-word.

---

## 4. Reported yesterday, still live after r6

`r6` did what its changelog says it did — the public-to-regulation channel opened on the 2026-05-05
reproduction of the Gilens–Page result (public coefficient ratio 1.31, not 0.039), and `T|S3` was
re-sized off the correct estimand (0.711 surviving, not 0.578). Both are edge-weight changes.
**None of yesterday's follow-through defects were touched, and all are on the live page:**

1. **The rail's "30-DAY DRIFT" column is wrong in all nine rows.** Live: Takeoff shape **+51.5pp**,
   Regulatory architecture **+24.8pp**. `drift30()` reads `h[len(h)-31]`; the history holds 25
   entries, so the index clamps to **0 = 2026-07-31**. Two things follow. **(a)** The measured span
   is **18 days, under a header that says 30** — the third sighting of this family in the project
   group. **(b)** K and R did not exist on 2026-07-31, and `(past[k]||{})[pos]||0` returns **0** for
   an absent axis, so a one-day-old axis renders its entire probability mass as drift. The other
   seven rows compare an r1/r4 position against an r6 position of the same letter: `P2 −8.0pp` sets
   r4's *adoption/acquiescence* against r6's *Stable disapproval* — near opposites.
2. **The mainline chip drops two of nine axes.** Live: `T2·A2·C1·D2·S1·P3·E3`. The seven-letter list
   is hardcoded in **three** places (`web/index.html` lines 445, 471, 990). K1 and R4 are invisible
   in every world-line label, including the exemplar rows — the comparisons `r5` created K and R to
   make possible.
3. **24 of 49 positions cannot be reached by any evidence rule** — all four K bands, all six R
   bands, P3, P4, P5, T5, C6–C8, A5–A7, D4, S4, S5, E5. Unchanged by r6. The rule library is still
   26 rules; **13 of them nudge C** and **0 nudge K or R**. At least six of those thirteen have a
   domestic or EU subject that `r5` moved to R: `ev-state-law-enacted`, `ev-state-bill-activity`,
   `ev-enforcement-action`, `ev-federal-preemption-push`, `ev-regulatory-implementation`,
   `ev-standards-published`.
   **The mainline is `T2·K1·A2·C1·R4·D2·S1·P3·E3` — and `K1`, `R4` and `P3` are all unreachable.**
   Three of the nine coordinates of the headline world-line are frozen priors that no evidence can
   ever revise. K and R also have **0 direct grounded pages** and are carrying a 1.25 widening
   factor, so their shape today is prior plus uncertainty spreading, and nothing else.

### New tonight

4. **`marginals.history` has duplicate and out-of-order dates.** 25 entries covering **19 distinct
   dates**; `2026-08-17` appears **four times**, `08-13`, `08-16` and `08-18` twice each, with two
   backward steps (`08-17 → 08-16`, `08-18 → 08-17`). Entries are appended without keying on date,
   so the r5/r6 development re-runs each left a row. `history[-1]` is a *same-day* entry, which is
   why the honest day-over-day figures in §3 had to be computed against the 08-17 nightly commit
   rather than against the file. Any surface reading this series by index against its dates draws a
   saw-tooth over the last week.

---

## 5. What I did not do

**I did not remap the crisis board or the claims register, and did not redeploy.** Each remap is a
modelling decision, not a mechanical one: whether *"no superintelligence this window"* means `T5`
alone or `T4 + T5` depends on where the window is taken to end; whether *"the bubble deflates hard"*
is `E3` or `E3 + E4` is a judgement about what "hard" means. Rewriting them would have silently
changed six published probabilities on August's forecast without review. Same reasoning for the
claims register, where `cl-state-laws-2026` needs a decision about which R positions constitute
"exceeds 90", not just a letter swap.

No wiki data defect was involved in any of tonight's findings — every one of them is in this repo
(`Research/timelines/forecast_emit.py`, `Research/timelines/axes.py`, `web/index.html`), so **no
task was filed into the vault queue.**

Suggested order for the next round, cheapest and most load-bearing first:

1. **Key the crisis board and claims register to positions by meaning, and make the build refuse
   when a referenced position's label has changed since the reference was written.** This is the
   same guard the Forecast Works build got on 2026-08-17 (exit 4 on a coverage declaration carrying
   axes, positions *and* meanings). It is the only fix that stops the next rebuild doing this again.
2. **Give the drift column a window it actually measured**: pick the baseline by date, not by index;
   return "—" for an axis absent at the baseline instead of subtracting from zero; and refuse to
   compare across a position-space version change.
3. **Derive every world-line label from `D.network.axes`** — delete all three hardcoded lists.
4. **Key `marginals.history` on date** (last write wins) and sort on emit.
5. **Route the six domestic/EU rules onto R**, and write the first rules that can reach K.
