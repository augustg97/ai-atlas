# Morning report — 2026-09-03 (Thursday)

**Chain: published and live-verified, exit 0.** `nightly.sh` printed `nightly OK 2026-09-03-1051`
and the in-chain check returned `live: DATA_V = "20260903-1050" / want: DATA_V = "20260903-1050"`.
Commit `3027be2` "Nightly emit 2026-09-03" is on `origin/main`, working tree clean, HEAD ==
origin/main (0 ahead, 0 behind). Wall clock 3m38s (10:47:38 → 10:51:16), of which 155.4s was the
embedding refresh.

Verified again afterwards and independently of the stamp: all eleven served
`data/forecast/*.json` return 200 and parse; the served `network.json` carries `date 2026-09-03`,
`version r10-2026-09-03`, 11 axes and 61 positions; the served `mainline.json` matches the staged
file exactly on both lines and both probabilities (medoid `wl` and `p`, and `argmax.wl` and
`argmax.p`, all compared as parsed values, all four equal).

Both 2026-08-20 preflight checks clean: `pgrep -f forecast_emit` returned nothing before the run,
and the working tree was empty at start. `Research/staged/forecast/` is one coherent frame — the
eleven files were written 10:50:39–10:50:40 by the single `forecast_emit` call that exited 0, with
`grounding.json` at 10:50:26 from the `wiki_grounding` step 13 seconds earlier in the same run.
Vault resolved at the primary location (`~/Vaults/August's Vault`).

---

## 0. The headline number changed meaning tonight — it did not collapse

**This is the first nightly emit to reach `docs/` under registry r10, and `mainline.json` no longer
means what it meant yesterday.** Read naively, the served `p` went `7.4968e-06 → 9.2891e-07`
overnight — an eightfold fall. Nothing of the sort happened.

The r9/r10 merge (`4ad2f43`, 2026-09-02 22:32) rewrote `worldlines.py`, `axes.py` and
`forecast_emit.py` and re-emitted `Research/staged/` — **but `git show --stat 4ad2f43 -- docs/`
is empty.** It never rebuilt the site. So the live `mainline.json` went on serving the r8-shape
object (a bare `wl` + `p`, no `kind`, no `agree`, no `argmax`) for a further day, and tonight's
build is the first to publish the new shape. The file now carries **two lines**:

| field | line | p |
|---|---|---|
| `wl` (`kind: "medoid"`, `n` 2000, `agree` 0.2959) | T3·K1·A4·C1·R4·D2·S3·P3·E3·L4·G2 | 9.28911762706215e-07 |
| `argmax.wl` | T2·K1·A2·C1·R4·D2·S1·P3·E3·L4·G4 | 7.460529366125291e-06 |

Yesterday's served line — T2·K1·A2·C1·R4·D2·S1·P3·E3·L4·G4, p 7.496809694709613e-06 — **is
tonight's `argmax`, position for position.** So the comparable quantity is:

- **argmax line: unchanged, all eleven positions.**
- **argmax p: 7.4968e-06 → 7.4605e-06, −0.484%.** An ordinary night's move.

The medoid is a *new* headline, not a moved one, and its lower probability is a property of being a
medoid: it is the ensemble's most central line, and centrality does not buy peak density. It also
matches the r10 medoid named in `HANDOFF.md` exactly, so nothing drifted between the branch and the
first nightly. **The 8× is a units change, not a forecast change**, and any chart that plots the
served `p` across 09-02 and 09-03 is plotting two different quantities on one axis.

This is the same defect class as the window-label bug: a field kept its name while the thing behind
it changed. Worth considering whether `mainline.json` should version its own shape, or whether the
argmax p should be the one carried in a series.

`weights.json` moved `r8-2026-08-20 → r10-2026-09-03` on this run, which is the durable record that
the registry revision advanced here and not at the merge.

**The registry step did not contaminate tonight's evidence/grounding split, and this was measured,
not assumed.** The merge left `weights.json` untouched, so there is exactly one 09-02 history entry
and it was written by the r8 nightly — its T marginals match the r8-era served `marginals.json` at
`4ad2f43` to five places. Tonight's `moved` therefore measures an r10 state against an r8-computed
baseline, which is the shape of a real contamination risk. Tested directly: recomputing that same
pre-run `weights.json` through **today's** `axes.marginals(widened_registry(w, {}))` reproduces the
stored r8 `raw` block with **max absolute difference 0.000e+00 across all 11 axes and 61
positions**. r9/r10 changed the worldline and template layer and left the belief network's
marginals exactly invariant, so the split below is clean and `delta.json`'s `attributable: true` is
earned.

---

## 1. Counts

```
embed         99 files · 969 chunks · 155.4s (skipped unchanged 3037)
datum         placed 19 · missing_unplaceable 0 · v1-2026-07-31 · max_drift 0.0
              0 unpositioned · 0 unterritoried
emit          entities 1010 · people 305 · events 2432 · arcs 14342
              series 564 · wikitime_ops 784 · needs_review 175
review        175 items · 7.196%
grounding     direct 1626 · corpus 453 · total 2079
              direct pages/axis  A 578 · C 410 · D 144 · E 194 · G 82 · K 0 · L 0
                                 P 229 · R 0 · S 261 · T 645
              widening (thin)    K 1.25 · R 1.25 · L 1.25 · G 1.144 · D 1.064
witness       matched 20 · disagreements 4 · gaps 133   (wiki_models 74 · epoch_rows 1041)
statelaw      observed 8 · states 7 · 0 bad
windows 0 bad · cards 0 bad · datekinds 0 bad · eras 0 bad · forecast 0 bad
```

Night-over-night: events 2413 → 2432 (+19) · entities 1001 → 1010 · arcs 14134 → 14342 · series
559 → 564 · wikitime_ops 781 → 784 · needs_review 175 → 175 · wiki_models 72 → 74. Grounding
direct +26, spread A +7 · C +6 · E +9 · S +3 · T +8; corpus 459 → 453 (−6). **K, R and L still
have zero direct pages** and stay pinned at the 1.25 widening ceiling — the same three axes as
every night this month, and the standing reason the G/D readings move on grounding alone.

19 new pages were projected into the frozen datum with `max_drift 0.0` — a placement, not a
re-layout.

## 2. The gate

**GATE: PASS (baselines hold).** The ratchet tightened three baselines on improvement:

```
events.target_pct          82.5 → 82.6
review.pct                7.252 → 7.196
forecast.grounded_direct   1600 → 1626
```

**`events.explicit_pct` eroded, 90.9 → 90.8 — and this is now the tightest it has ever been.**
Frozen 07-31 baseline **91.7**, hard refusal below **90.7** (`audit_all.py:270`, `baseline − 1.0`):
0.9 of the 1.0pp band spent, **0.1pp of headroom**. Yesterday it held at 90.9 for a fourth night
and the report called 0.2pp. Tonight it moved the wrong way. One more 0.1pp step and the metric
sits on the edge; two and the gate refuses and the site does not publish.

The denominator grew by 19 events tonight, so a single night of low-explicitness ingest moves this.
It is not eroding fast, but it no longer has room to absorb a bad night, and it is the one number
in the gate that can stop a publish without anything being wrong with the model.

## 3. Forecast deltas

```
fresh events 19 · distinct incidents 19 · applied 3 · residue 16 · collapsed 0
contested 0 · recovered late 10 (event dates 2026-09-01 … 2026-09-02)
seen-ledger 561 → 580 · evidence_log 164 → 167 · schema_additions 0
```

**No SCHEMA ADDITIONS block — today is Thursday and the schema review runs Mondays only.**
`schema_additions` is 0 and nothing was auto-added.

The three applications, with their drivers:

- **ev-frontier-release** [minor ×0.00145 · k=1.84 · spread=1.00 · 1 report · 2 sources] — Meta
  released Muse Spark 1.3 in Muse Code and the Meta Model API on September 2, 2026.
- **ev-capital-commitment** [minor ×0.00048 · k=10.59 · spread=0.71 · 1 report · 3 sources] —
  Nvidia announced a definitive agreement to acquire Hugging Face for $12.93 billion, September 3,
  2026.
- **ev-capital-commitment** [minor ×0.00032 · k=10.59 · spread=0.71 · 1 report · 1 source] —
  Nscale telling prospective investors it holds about $103 billion in total contracted revenue.

Net movement in the marginals, `shown = evidence + grounding`:

```
evidence   T.T3 +0.0016635  T.T4 −0.0011325  T.T2 −0.0003206  T.T5 −0.0001148  T.T1 −0.0000957
           E.E1 +0.0009420  E.E3 −0.0007040  E.E4 −0.0001005  E.E2 −0.0000952  E.E5 −0.0000423
grounding  D.D4 +0.0006882  D.D2 −0.0009710  D.D1 +0.0001914  D.D3 +0.0000914
           G.G5 +0.0002272  G.G2 −0.0004513  G.G6 +0.0001440  G.G3 +0.0000873  G.G4 +0.0000873
           G.G1 −0.0000945
```

Only T and E moved on evidence; D and G moved on grounding alone, which is the widening factors
shifting (D 1.055 → 1.064, G 1.139 → 1.144) as direct pages accrued elsewhere. **The two
capital-commitment applications both pushed E away from correction and toward continued buildout**
— E1 up, E3 and E4 down.

The evidence column reconciles against the three requests once renormalisation is accounted for.
The requests were `T.T3 +0.0019374 / T.T4 −0.0009687` (Meta) and, summed over the two capital
entries, `E.E1 +0.0010645 / E.E3 −0.0005323`; those are applied to unnormalised weights and the
axis is then renormalised, which lands T.T3 at +0.0016635 and E.E1 at +0.0009420, and pushes E.E3
to −0.0007040 — further than requested, because E1's rise takes mass from the whole axis. `moved`
is one night's motion, not the delta window's: it is computed against the last history entry with a
date other than today, and `delta.json`'s 40 `entries` (2026-08-26 … 2026-09-03) are a rolling
display window, not the basis of this arithmetic.

**Residue is 16 of 19 — 84%, against 65% (13 of 20) yesterday.** Diffed against the previous
`weights.json`, the 16 new entries carry event dates 09-01 (2), 09-02 (7) and 09-03 (7), and they
are spread across **ten** sections: Reported but unverified 3 · AI Litigation 2 · AI Adoption 2 ·
AI Industry & Markets 2 · Federal AI Policy 2 · Frontier Models 1 · AI Safety 1 · Compute &
Chips 1 · U.S. State AI Legislation 1 · National Security 1. **No cluster** — this is a thin
matching night, not a systematic blind spot, and there is nothing here that Monday's residue review
would find as a sustained cluster on tonight's evidence alone. The banked residue stands at 406 of
its 500 cap.

## 4. Calibration

**No claim has passed its deadline and none resolved today.** All four remain `open`. Nearest
deadline is 2026-12-31, ~4 months out.

| claim | by | status | where it stands tonight |
|---|---|---|---|
| `cl-state-laws-2026` — state AI-law count exceeds 90 | 2026-12-31 | open | witness `observed 8 · states 7`, **unchanged**; R2 = 0.248, unmoved |
| `cl-sc-2027` — superhuman-coder milestone crossed | 2027-12-31 | open | T1 = 0.099, moved −0.000096; no position crosses rung 3.0 by 2027 |
| `cl-correction-2027` — major AI capex correction | 2027-12-31 | open | E3 = 0.322 (−0.000704), E4 = 0.189 (−0.000101) |
| `cl-deal-2029` — US-CN AI agreement concluded | 2029-12-31 | open | C3 = 0.132, unmoved |

Two things worth recording rather than letting pass:

- **Tonight's record moved evidence against `cl-correction-2027`.** Both capital-commitment
  applications — Nvidia/Hugging Face at $12.93bn and Nscale's claimed $103bn contracted revenue —
  pushed E toward buildout and away from correction. Both of the claim's supporting positions fell.
  This is a nudge, not a resolution, and the claim has 16 months to run.
- **`cl-state-laws-2026` still cannot be resolved by the machinery that watches it.**
  `witness_statelaw` says so itself: it counts wiki-observed enactments, which is a lower bound on
  the claim's universe, so it "can confirm the claim if it alone passes the threshold; it can never
  refute it." At 8 against a threshold of 90 it will not confirm. The one state-legislation item in
  tonight's record — Anthropic supporting the AI language in the Massachusetts Senate's economic
  bill — is an endorsement, not an enactment, and correctly went to residue rather than the counter.
  **Resolving this claim needs NCSL or an equivalent named tracker ingested as a witness**, and with
  the deadline four months out that ingest is the thing standing between this claim and an
  unresolvable expiry.

## 5. Standing items

- **`events.explicit_pct` at 0.1pp of headroom** (§2) — the nearest thing to a blocked publish.
- **The `mainline.json` shape change** (§0) — the served `p` is not comparable across 09-02/09-03.
  Consider versioning the file's shape, or carrying `argmax.p` as the series.
- **A re-emit that skips `build_site.py` leaves `docs/` a day stale and writes no history entry.**
  The r10 merge did both. Nothing broke — the registry turned out to be marginals-neutral — but the
  same sequence on a registry change that *did* move the marginals would have charged a one-off
  registry step to "evidence" in the morning report, invisibly.
- **K, R and L still ground on zero direct pages**, pinned at the 1.25 widening ceiling.
- Four date disagreements persist between wiki and epoch, unchanged: `alphafold` (+320d),
  `deepseek-r1` (−126d), `deepseek-v3` (−113d), `gemini-2-5-pro` (−72d). 133 frontier gaps.
- Two INGEST task files still sit unprocessed in the vault queue
  (`amazon-rehearing-en-banc-petition-2026`, `brinkema-adtech-remedy-memorandum-2026`).
