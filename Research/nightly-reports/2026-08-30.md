# Morning report — 2026-08-30 (Sunday)

**Chain: published and live-verified, exit 0.** `nightly.sh` printed `nightly OK 2026-08-30-1049`,
the in-chain stamp check returned `live: DATA_V = "20260830-1048" / want: 20260830-1048`, and
commit `38f2870` "Nightly emit 2026-08-30" is on `origin/main` (working tree clean, HEAD ==
origin/main). Verified again afterwards and independently of the stamp: the served
`data/forecast/network.json` carries `date 2026-08-30`, `version r8-2026-08-20`, 11 axes, and all
nine served forecast files parse. The live page renders (screenshot taken; see §3 — it renders
two things that are not true).

Both 2026-08-20 preflight checks clean: `pgrep -f forecast_emit` returned nothing before the run,
and the working tree was empty at start. `Research/staged/forecast/` is **one coherent frame** —
ten files at 10:48:41 with `grounding.json` at 10:48:30. Vault resolved at the primary location
(`~/Vaults/August's Vault`).

---

## 1. Counts

```
events        n 2364 · explicit 90.9% · target 82.4%
emit          entities 992 · people 303 · arcs 13927 · series 556 · wikitime_ops 768
review        175 items · 7.403%
grounding     direct 1578 · corpus 462 · total 2040
              direct pages/axis  A 556 · C 398 · D 144 · E 182 · G 82 · K 0 · L 0
                                 P 224 · R 0 · S 257 · T 627
              widening (thin)    K 1.25 · R 1.25 · L 1.25 · G 1.137 · D 1.052
witness       matched 20 · disagreements 4 · gaps 133
statelaw      observed 8 · states 7 · 0 bad
datum         v1-2026-07-31 · max_drift 0.0 · 0 unpositioned · 0 unterritoried
windows 0 bad · cards 0 bad · datekinds 0 bad · eras 0 bad · forecast 0 bad
```

**GATE: PASS.** Ratchet tightened three baselines — `events.target_pct` 82.2 → 82.4,
`review.pct` 7.437 → 7.403, `forecast.grounded_direct` 1571 → 1578.

**`events.explicit_pct` held at 90.9** — no further erosion tonight. Frozen 07-31 baseline
**91.7**, hard refusal below **90.7**: 0.8 of the 1.0pp band spent, **0.2pp of headroom**. Still
the metric closest to a hard refusal.

Forecast pass: **11 fresh events · 11 distinct incidents · 2 applications · 9 to residue · 0
collapsed · 0 contested · 6 late arrivals recovered** (event dates 2026-08-25 … 2026-08-29) ·
seen-ledger 512 · **schema_additions 0**. No SCHEMA ADDITIONS block — today is Sunday and the
review runs Mondays; the last addition was `D.watch-labor` on Monday 08-24.

**Mainline unchanged** — `T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4`, p 7.52986e-06 → 7.53076e-06
(+0.012%). All eleven axes held. `G4` holds a tenth night; the r8 note's provisional reading of G
still stands, and G again moved only from the widener.

**Registry drift, checked by hand: none.** Axes 11 → 11, positions 61 → 61 with no position
renamed, sub-axes 8 → 8 with identical keys and origin flags (5 auto, 3 authored), conditional
axes 10 → 10 over 213 → 213 rows — diffed against last night's emitted `network.json`, not the
seed module, per the 08-29 method note. The unchanged version string `r8-2026-08-20` is truthful
tonight. `registry_drift()` **still does not exist in the repo**; grepped `build/` and `Research/`
again, the only hits are prior reports saying so. **Seventh night of hand checks.**

---

## 2. Forecast deltas

Total position motion **0.3245pp** — 52.8% evidence, 47.2% grounding. By axis:

```
S  0.1713 pp   evidence 0.1713   grounding 0.0000
G  0.1095 pp   evidence 0.0000   grounding 0.1095
D  0.0437 pp   evidence 0.0000   grounding 0.0437
```

**Eight of eleven axes did not move at all** — T, K, A, C, R, P, E, L. Every unit of evidence
motion in the model tonight landed on one axis, S.

Movers:

```
S.S2  +0.0856 pp (evidence)      G.G2  +0.0453 pp (grounding only)
S.S3  -0.0604 pp (evidence)      G.G5  -0.0228 pp (grounding only)
S.S1  -0.0114 pp (evidence)      D.D2  +0.0219 pp (grounding only)
S.S4  -0.0090 pp (evidence)      D.D4  -0.0154 pp (grounding only)
S.S5  -0.0048 pp (evidence)      G.G6  -0.0144 pp (grounding only)
```

Both applications, with drivers:

| rule | class | ×mag | k | spread | driver |
|---|---|---|---|---|---|
| `ev-compute-buildout` | minor | 0.0003616 | 5.376 | 0.707 | SpaceX assembling a foundry in Bastrop, Texas to cast gas-turbine blades and vanes (event 08-29) |
| `ev-compute-buildout` | minor | 0.0003616 | 5.376 | 0.707 | Meta testing robots that plug in cables and reset servers inside its data centres (event 08-28) |

Same rule twice, identical magnitude, identical fixed nudge `S2 +2 / S3 −1`. See §4 — neither
event is about what the rule scored it as.

---

## 3. Finding — four of the eleven rows in the "30-day drift" panel are not drift

This is user-facing and it is the first panel a reader's eye lands on. `web/index.html:983`:

```js
function drift30(k,pos){
  const h=D.marginals.history;
  if (h.length<2) return 0;
  const past=h[Math.max(0,h.length-31)].marginals;
  return ((activeMarginals()[k][pos]||0)-((past[k]||{})[pos]||0))*100;
}
```

I reproduced all eleven live rows from this function to the displayed digit. Two defects sit in
those three lines.

**(a) The baseline is a row index, not a date.** `h[h.length-31]` counts rows. `marginals.history`
holds **37 entries over 31 distinct dates** — 08-13 stored twice, 08-16 twice, 08-17 four times,
08-18 twice — so 31 rows back lands on **2026-08-06**, which is **24 calendar days** ago. The
heading says 30-day. This is the un-deduplicated history recorded on 08-19, now with a
user-visible consequence rather than only a weighting one.

**(b) An axis that did not exist at the baseline reads as zero.** `((past[k]||{})[pos]||0)`. K, R,
L and G were all created after 2026-08-06 — K and R at r5 (08-17), L at r7 and G at r8 (both 08-20). For
those four the subtraction is `current − 0`, so **the panel renders the axis's entire present
probability as if it were movement**:

```
axis  modal position                       current   baseline   panel shows
K.K1  Takeoff shape: rungs inside 1 yr      51.48pp   ABSENT      +51.5pp
G.G2  Benefit realisation: rich absorb it   25.89pp   ABSENT      +25.9pp
R.R2  Regulatory arch.: contested patchwork 24.75pp   ABSENT      +24.8pp
L.L4  Lab conduct: commercial cadence       22.57pp   ABSENT      +22.6pp
```

The seven real rows reproduce too, and are genuine: T +3.00, A −2.80, C −7.16, D −17.77,
S −0.12, P −7.96, E +12.03 (panel: +3.0, −2.8, −7.2, −17.8, −0.1, −8.0, +12.0).

Ranked by magnitude, the panel reads K +51.5, G +25.9, R +24.8, L +22.6, D −17.8, E +12.0 … —
**the four largest numbers on the board are the four fabricated ones**. They sum to 124.8pp of
the 175.7pp of absolute magnitude the panel displays — **71% of everything the reader sees
there is an artefact of a missing baseline**. A reader taking the panel at face value concludes that
takeoff shape and benefit realisation are the fastest-moving things in the model. Neither has
ever received an evidence write.

**This is the 2026-08-17 defect, unrepaired on its thirteenth day, and it has grown.** It was
recorded then as two axes at +51.5 and +24.8 — the same two numbers, unchanged, because a frozen
zero baseline makes the "drift" equal to the current probability and K1 and R2 have barely moved
since. r7 and r8 each added an axis, and **each new axis joins this panel with a fabricated row**.
The defect grows by one row per registry revision, automatically, with nothing to stop it.

**(c) A third, separate one, four lines below** (`web/index.html:992`): the mainline chip is built
from a hard-coded seven-axis literal.

```js
$("lineChip").textContent = ["T","A","C","D","S","P","E"].map(k=>wl[k]).join("·")
```

So the rail reads `T2·A2·C1·D2·S1·P3·E3 (mainline)` while the mainline is eleven positions,
`T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4`. **K, R, L and G are silently dropped** — including `K1` at
51.5%, the single most probable position anywhere in the model, and `G4`, the reading r8 flagged
as provisional. The panel two inches to the right advertises those same four axes as the biggest
movers on the board. Same seven-axis-literal class as the crisis board and `worldlines._selftest`,
in a surface nobody has swept.

---

## 4. Finding — both of tonight's applications moved the compute axis against their own content

Only two events matched anything, both to `ev-compute-buildout`, whose nudge is fixed at
`S2 +2 / S3 −1` — S2 is "Capital binds, capacity diversifies", S3 is "Power and siting bind".

**The SpaceX foundry.** Matched on `foundry`. The item, in full, is a power story and says so
repeatedly: SpaceX is casting its own gas-turbine blades and vanes because the four-company
casting oligopoly's "maxed-out foundries are a reason turbine makers including GE Vernova are sold
out until 2030"; Musk is quoted that blades and vanes are "the limiting factor" in provisioning
turbines and that "it's not clear to me that there's enough usable electricity to turn on all the
AI chips that are being made." The source URL slug is
`…lays-groundwork-turbine-blade-factory-solve-data-center-power-crunch`. The digest lede calls it
"an attempt to route around the component shortage constraining data-center power."

This is about as direct a statement of S3 as the record can produce. **The model moved S3 down
0.024pp and S2 up 0.048pp.**

The rule has a guard for exactly this — `text_none` includes `shortage*`, `curtailment*`,
`halted`, `delayed`. It did not fire, and **not because of truncation**: the matcher sees
`plain[:300]` and the item runs 2,551 characters, but I searched all 2,551 and **not one guard
word appears anywhere in the item body**. The record states the constraint as "the limiting
factor", "sold out until 2030", "maxed-out foundries", "not enough usable electricity". The guard
is a fixed vocabulary; the record is prose. Reading the full text would not have saved this one.

**The Meta robots.** Matched on `data center*`. The item is about robots doing technician work —
"a successful cable-swapping bot could replace up to 80 percent of some people's workloads" — and
carries a direct denial from Meta ("we need more workers, not fewer") and a practitioner saying
there is "no provable working solution". It is a labour-displacement item with a contested
reading, filed under `Compute, Chips & Infrastructure`. It moved **S**. It put nothing on **D**
(Diffusion and labour), which tonight moved only from the widener, and it registered `contested: 0`.

That is the 2026-08-26 section gate again with the polarity reversed: on 08-26 the section decided
whether an item was heard at all; tonight it decided **which axis** heard it, and picked the wrong
one.

### The wiring underneath

Across 145 lifetime applications, direct writes to the S positions:

```
S.S2   ev-compute-buildout 18 · ev-compute-constraint 2
S.S3   ev-compute-buildout 18 (all down) · ev-compute-constraint 2 · ev-export-retaliation 1
S.S1, S.S4, S.S5   never written by any rule, ever
```

**S.S3 has been written down 18 times and up 3, and every one of the 18 down-writes is the same
rule.** S3's only three raisers are `ev-export-retaliation` (1 firing lifetime, and per the 08-21
finding that one was itself backwards), `ev-compute-constraint` (2), and
`ev-distillation-attack` (never fired). Three of the five positions on the compute axis are
unreachable by any rule in the library — the axis's entire evidence history is one pair.

So the direction defect here is not one bad night. The only rule with volume on this axis pushes
one way by construction, and tonight it pushed that way on an item whose own source URL names the
opposite.

Novelty context: `k = 5.376` → novelty 0.1705, **13.6% above the 0.15 floor**. `ev-compute-buildout`
is at 18 lifetime applications and, as last night established, past k ≈ 7 further repetition is
free; this rule is approaching that.

---

## 5. Calibration

**No claim has passed its deadline, and none resolved against tonight's record.** Nearest is
`cl-state-laws-2026` (by 2026-12-31, **123 days out**).

The witness is unchanged: **8 observed enactments across 7 states**, declared a lower bound —
it can confirm the claim if it alone passes 90 and can never refute it. The three-number tension
recorded on 08-23 stands unchanged: R-axis description "109 AI laws in H1 2026" (flow), 61
(stock), threshold 90, witness 8.

Nothing tonight bore on `cl-sc-2027`, `cl-deal-2029` or `cl-correction-2027`. The wiring table
from 08-29 is unchanged — **no claim has a support position that any ever-fired rule can raise**,
and `R.R2` remains inert with no rule touching it in either direction.

One thing worth naming: tonight's residue holds **Amazon's definitive agreement to acquire
DuckLabs** (08-26). That is the second $-scale acquisition to land in residue in two nights, the
night after the M&A blindness was written up. `cl-correction-2027` is about a capex correction and
consolidation is among the most direct evidence it could receive; the model cannot see it.

---

## 6. The residue is again a coverage map

Nine banked. Five are tonight's, four are recovered late arrivals:

```
08-30  Apple Mac mini/Studio fastest-growing business on AI-lab demand   [Compute]
08-30  Salesforce lets businesses choose how they pay for Agentforce     [Industry]
08-30  Emanuel/Khosla et al. in JAMA on AI in medicine                   [Adoption]
08-30  Oliver Patel's analysis of China's agentic AI governance framework [Intl Reg]
08-30  Employers adding screening steps to prove candidates are real     [Labor]
08-27  OpenAI developing a proactive persistent version of Codex         [Agentic]
08-27  Allen Institute flagged an immunotherapy signal in a breast
       cancer subtype oncologists had written off                        [Adoption]
08-26  Amazon agreed to acquire DuckLabs                                 [Industry]
08-25  Pew: 34% of U.S. adults use AI chatbots at least weekly           [Labor]
```

Read against the axes that stood still: the **China governance** item is R evidence and R has never
received an evidence write in 145 applications. The **Allen Institute cancer signal** and the
**JAMA** piece are both G — a measured clinical benefit is precisely G's subject — and G has 3
lifetime writes, none tonight. The **Pew survey** is P, which has 3 lifetime writes, all to P2.
The **job-screening** item is D. Four axes sat motionless tonight while their own evidence was
banked as unexplained.

`ev-benefit-delivery`, `ev-clinical-readout` and `ev-clinical-shortfall` are all in the library and
all have never fired. The Allen Institute item is the kind of thing `ev-clinical-readout` exists
for.

---

## 7. Standing items

1. **The drift panel publishes four fabricated rows** (§3) — new tonight as a measured finding,
   thirteen days old as a defect, and it grows by one row per registry revision.
2. **The mainline chip drops four of eleven axes** (§3c) — seven-axis literal at `index.html:992`.
3. **`registry_drift()` absent — seventh night** of hand checks. Tonight's: clean.
4. **`events.explicit_pct` 90.9, hard gate 90.7** — held, 0.2pp of a never-returned band.
5. **G reading provisional** — `G4` a tenth night; G moved tonight only from the widener.
6. **K, L, R have never received an evidence write.** Unchanged. R had evidence in the residue
   tonight.
7. **13 of 31 rules have never fired** (18 ever fired, 145 lifetime applications).
8. **`marginals.history` neither deduplicated nor date-ordered** — 37 entries, 31 dates. It now has
   a user-facing consequence as well as a weighting one.

## 8. What tonight argues for, in order

1. **Fix `drift30`, three lines.** Select the baseline **by date**, not by row index; and when an
   axis is absent from the baseline, render **"new"** rather than a number. Rule 10 already covers
   this: "unknown" is a legitimate return. This is the cheapest fix on the list and the only one
   the reader can currently see.
2. **Build the line chip from the live registry**, not a literal. Any hard-coded axis list is a
   time bomb the next revision arms.
3. **Split `ev-compute-buildout`, or let a rule read direction.** Its 18 firings are 18 votes that
   power does not bind, cast partly on stories about power binding. A `text_none` list cannot carry
   this — tonight's item states the constraint in four different phrasings, none of them in the
   guard. This is the 08-15 direction defect, and it now has its cleanest single instance.
4. **Route by content, not only by section.** The Meta robots item is D evidence sitting in a
   Compute section, and the section decided the axis.
5. **Dedupe and date-order `marginals.history`.** One pass, fixes §3a and the 08-19 weighting bug.
