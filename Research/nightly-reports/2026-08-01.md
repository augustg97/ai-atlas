# Morning report — 2026-08-01 (Saturday)

**Status: PUBLISHED + VERIFIED** · `DATA_V = 20260801-1048` · gate PASS (baselines hold) · exit 0
first attempt, no retry, no `SKIP_AUDIT`, no gate override.
Live stamp confirmed twice — by the chain (`live: … / want: …` matched) and by an independent
`curl` afterwards. Commit `aacf2fb`, working tree clean.

**Both acceptance signals HANDOFF asked me to watch for today landed** (§3, §4). Morning 2 of 3
(T4/D1) is correct *and* complete — unlike morning 1, the classify → tier → attribute → residue
core ran on a non-empty input set.

---

## 1. Counts

| stage | result |
|---|---|
| embedding refresh | 54 files / 424 chunks in 41.4s (2,631 unchanged, checksum-gated) |
| datum place-new | **16 pages** projected into frozen datum v1 · `max_drift 0.0` · 0 unplaceable |
| trunk emit | 871 entities · 270 people · **1,861 events** · 11,626 arcs · 430 series rows · 576 wikitime ops |
| needs-review | 224 (unchanged) |
| witness/epoch | 61 wiki models · 1,041 epoch rows · 20 matched · 41 unmatched · **133 frontier gaps** |
| nightly_update | **fresh 2 · applied 0 · residue 2 · schema additions 0** |
| grounding | **1,246 direct + 481 corpus = 1,727** (was 1,230 + 481 = 1,711 → **+16 / +0 / +16**) |
| gate | PASS · windows 0 bad · cards 0 bad · date-kinds 0 bad · eras 0 bad · forecast 0 bad · explicit dates 91.8% vs 80.7% target |

Mainline path unchanged: **T3 · A3 · C1 · D2 · S2 · P3 · E2**. Its joint probability moved
0.00216744 → 0.00216421 (−3.2e-6), consistent with D2 and P3 each shedding a little mass (§2).

## 2. Forecast deltas — and their driver

Measured against the pre-run state I snapshotted before the chain touched anything.

- **T, A, C, S, E — unchanged, exactly (0.000000 on every position).**
- **D:** D1 **+0.00034** · D2 **−0.00055** · D3 **+0.00021**
- **P:** P1 +0.00014 · P2 +0.00006 · P3 −0.00019

**Driver: grounding only — and specifically the *median*, not D's own page count.** No evidence was
applied (§4), so the one input that moved was `wiki_grounding`. Per-axis direct pages:

| axis | A | C | D | E | P | S | T |
|---|---|---|---|---|---|---|---|
| pre | 462 | 314 | **124** | 143 | 177 | 190 | 523 |
| post | 469 | 318 | **124** | 146 | 179 | 194 | 533 |

`thin_axes()` sets each axis's prior-widening factor from its deficit against the **median** axis
(`wiki_grounding.py:117`). The median axis is S, and S grew 190 → 194. **D gained no pages at all**,
so its deficit widened purely because everything around it grew: widen 1.087 → 1.090. P grew (+2)
but by less than the median (+4), so it widened slightly too: 1.017 → 1.019. E's unrounded factor
moved ~6e-5 but `grounding.json` stores it rounded to 3dp — 1.062 both days — so E's marginals came
out exactly zero. That rounding is why E is flat and D is not.

Widening pulls an axis toward uniform, which is exactly the signature in the numbers: on both D and
P the *largest* position (D2 0.533, P3 0.428 — both mainline) lost mass and the smaller ones gained.

These are ~1e-4 drifts. Honest reading: **the distribution did not meaningfully change today.** What
is worth noting is the mechanism — an axis can move on a morning when *its own* evidence base did
not change at all. D is the thinnest axis in the model (124 pages, static for two days running) and
it will keep drifting toward uniform every morning the rest of the wiki grows without it.

## SCHEMA ADDITIONS

**None.** Today is Saturday; `schema_review()` returns immediately on any non-Monday. The first
schema review runs **2026-08-03**. Nothing was auto-added, so there is no block to reproduce — this
line is recorded so the absence is not mistaken for an omission.

Forward-looking, and stated as a prediction so it can be checked: the review needs **12 unexplained
events in one section within 7 days** (`RESIDUE_CLUSTER_MIN`). Residue began accumulating only today
and stands at **2**, in two different sections. Monday will almost certainly add nothing. If it does
add something, one of my assumptions here is wrong and that is worth knowing.

## 3. The two acceptance signals — both landed

1. **Marginals history accrued.** `marginals.json.history` now holds **2** entries — 2026-07-31 and
   **2026-08-01**. The daily-accrual claim in HANDOFF is correct; yesterday's ⚠️ is cleared.
2. **The evidence core ran on real input.** fresh = 2 (not 0). The window
   `date > last AND date <= today` selected the two events dated 2026-08-01. Both banked as residue:

| section | event | outcome |
|---|---|---|
| AI Litigation, Liability & Enforcement | ACLU's Lauren Yu on unsettled US liability for rogue agents | residue |
| Frontier Models & Capabilities | OpenAI previewing "Astra" model family in DC (`kind: published`) | residue |

The 2026-08-02 EU AI Act Article 50 events (2 of them) were correctly deferred — they are outside
`<= today` and will be picked up tomorrow. Neither matches a rule.

## 4. ⚠️ The day-boundary is not a one-off — it is now costing real evidence, daily

Yesterday I flagged that the 05:10 seed had stamped `weights["date"] = 2026-07-31`, so that day's
7 events were skipped, and measured the cost at **0 of 7** rule matches — nothing lost. That
measurement was right and the conclusion drawn from it was too narrow. Today the same boundary is
measurably worse, for a structural reason:

**The chain stamps `weights["date"] = today` at 10:48, but the wiki keeps ingesting all day.**

- At 15:11 yesterday the trunk held **7** events dated 2026-07-31. It now holds **23**.
- All **16** that arrived after are permanently invisible: tomorrow's `last` is 2026-08-01, so
  nothing dated 2026-07-31 can ever qualify again.
- **Cost, measured not assumed:** dry-running the classifier read-only over all 23 —
  **1 would have matched a rule.** Illinois Gov. Pritzker signing HB 5511 fires
  `ev-state-law-enacted`, the library's highest-volume rule (50 hits across the trunk), which
  nudges **C4**. That is a real weight movement, lost. It also bears directly on the open claim
  `cl-state-laws-2026` (§5).

Today's own numbers show the same shape forming: only **2** events are dated 2026-08-01 at 10:48,
against a recent daily average of ~20. On present evidence roughly **90% of each day's record will
be skipped every day**, not once.

**I did not change the date semantics.** `>` → `>=` re-applies the same events nightly forever
(idempotency loss); a backfill changes which evidence enters the model. The clean fix is probably a
**one-day lag** — select `date <= today − 1` and stamp `weights["date"] = today − 1`, so only settled
days are classified — but that is a model-semantics call, and it is August's. **Flagging, not
fixing**, as yesterday.

## 5. The evidence-rule library, measured across the whole trunk (T9)

0 of 2 fresh events matched today, as 0 of 7 did yesterday. Rather than report that ratio a third
time, I measured the library against the entire record:

- **66 of 1,861 events (3.55%) match any of the 11 rules.**
- **5 of the 11 rules match nothing at all** in the whole trunk: `ev-big-correction`,
  `ev-us-cn-agreement`, `ev-autonomous-rd-demonstrated`, `ev-sabotage-ops`, `ev-moratorium-movement`.
  `ev-state-law-enacted` alone accounts for 50 of the 66.
- **721 events (38.7%) sit in sections no rule can reach**, because rules match on an exact section
  string and 15 section labels have no rule. The largest uncovered sections are
  **Compute, Chips & Infrastructure (209)**, **AI Safety, Alignment & Interpretability (152)**,
  **AI Litigation, Liability & Enforcement (136)**, **Agentic AI & Coding (110)**,
  **AI Adoption by Industry (94)**. The model has an S axis and an A axis and cannot currently
  learn anything about either from the record.
- **Near-miss worth citing:** today's Astra event is semantically what
  `ev-frontier-release-gov-coordinated` is for — a frontier model previewed to policymakers and
  regulators in Washington — but the rule needs the literal substring "government-coordinated" or
  "limited preview", and the text has neither. Substring matching is the binding constraint, not
  just rule count. Raising the library to ~40 rules without addressing that will not help
  proportionally.

## 6. A wiki data defect, flagged not filed

Eight section labels appear **exactly once each** and are near-duplicates of the canonical
13: `AI Markets` / `AI Adoption & Biometric Identity` / `AI Safety, Liability & Security` /
`International Governance` / `International AI Regulation & Geopolitics` /
`U.S. Federal Policy & Geopolitics` / `U.S. State AI Legislation & Litigation` /
`State Legislation & Regulation` / `Social Media Governance`. All are from April–June; all
silently defeat exact-string rule matching and section-based routing.

This is a wiki-side normalisation defect, not a coverage limit — the queue is the right path for
it. **I did not file it.** The one sanctioned vault write is scoped to gate refusals caused by a
data defect, and the gate passed. Volume is 9 events, so nothing is urgent. Recorded here for
August to authorise.

## 7. Calibration check

Four claims in `Research/staged/forecast/claims.json`, all `status: open`. **None past deadline,
none resolved.** One moved, in a way that must not pass unrecorded:

| id | claim | deadline | today |
|---|---|---|---|
| `cl-state-laws-2026` | state AI-law count exceeds 90 | 2026-12-31 | **evidence appeared and was not counted** — Illinois HB 5511 signed 2026-07-31 is exactly this claim's currency, and it fell into the §4 gap. Still open, still 5 months out. |
| `cl-sc-2027` | superhuman-coder milestone crossed | 2027-12-31 | open · today's Astra item (long-running tasks, multi-agent) is directionally relevant but is a *preview report*, `kind: published`, not a milestone. Not a resolution. |
| `cl-correction-2027` | major AI capex correction | 2027-12-31 | open, unmoved |
| `cl-deal-2029` | US-CN AI agreement concluded | 2029-12-31 | open, unmoved |

No resolution passed unrecorded. The Brier scorer is still T7.

## 8. Carried forward

- **For August, and now with a price attached:** the day-boundary (§4). One real rule-matching
  event has already been lost and ~90% of each day's record is on track to follow. Nothing changes
  until you rule on lag-vs-backfill.
- **For August:** authorise (or decline) a queue task for the 9 section-label variants (§6).
- **Morning 3 of 3 is 2026-08-02.** Watch: the two deferred EU AI Act Article 50 events entering
  the window; a third marginals history entry; and whether fresh count is again ~2 rather than ~20,
  which would confirm §4 as steady-state rather than a Saturday artefact.
- 4 witness/epoch date disagreements persist (alphafold ±320d, deepseek-r1 −126d, deepseek-v3
  −113d, gemini-2-5-pro −72d) and 133 frontier gaps. Unchanged, gate-clean, known coverage limits.
- Unmatched wiki models rose 36 → 41 as 5 new model pages landed without epoch rows. Expected,
  gate-clean.
- 224 needs-review items unchanged.

*No queue task filed: the gate passed and no defect blocked the chain (§6 is flagged for
authorisation, not filed).*
</content>
