# Morning report — 2026-08-19 (Wednesday)

**Chain: exit 0 — published and live-verified.** `DATA_V = "20260819-1051"` requested and
returned by `https://augustg97.github.io/ai-atlas/`. Commit `7c1cf71` "Nightly emit 2026-08-19".
Vault resolved at the primary location (`~/Vaults/August's Vault`); the legacy iCloud path is
absent, which is expected.

---

## 1. Calibration — the claims register (§3 of the task)

**No claim has passed its deadline.** The nearest is `cl-state-laws-2026` (by 2026-12-31, 134
days out). Nothing resolved visibly against today's record. But there is a tension inside
tonight's own build that should not pass unrecorded:

- **`cl-state-laws-2026` — "state AI-law count exceeds 90".** The live witness
  (`witness-statelaw.json`) counts **8 enactments across 7 states**, and says so honestly: a
  lower bound on the claim's universe, able to confirm but never to refute, because the wiki
  tracks policy-notable developments rather than fifty states' legislative output. Resolving it
  needs NCSL or an equivalent named tracker ingested as a witness.
- **The same build contains a number that would cross the threshold.** The `R` axis description
  in the live registry (`r6-2026-08-18`) reads: *"United States states enacted 109 AI laws and 28
  data-center statutes in the first half of 2026 from 1,561 bills across 45 states."* 109 > 90,
  in H1 alone. That figure is not wired to the claim, and I cannot tell from inside the build
  whether it counts the claim's universe or a wider one — which is precisely the question the
  named-tracker witness exists to settle. **Recorded as an open tension, not a resolution.**
- Its `context_only` note also carries the 2026-04-30 NCSL reading of 12 states legislating on
  healthcare-and-AI, correctly marked `comparable: false` (domain-scoped; the claim is not).

**Separately: `cl-state-laws-2026` draws its probability from `C4`,** which in r6 means
"Domain-confined limit" — a rung on the US–China coordination ladder. The claim is about US state
legislatures. The r6 home for it is `R2` "Contested patchwork". Detail in §4.

---

## 2. Counts

```
events        n 2180 · explicit 91.4% (target 81.4%)
review        210 items · 9.633%
grounding     direct 1437 · corpus 486 · total 1923
              direct pages/axis  A 520 · C 362 · D 129 · E 160 · K 0 · P 203 · R 0 · S 229 · T 591
              widening (thin)    K 1.25 · R 1.25 · D 1.091 · E 1.053
witness       matched 20 · disagreements 4 · gaps 133
statelaw      observed 8 · states 7 · 0 bad
datum         v1-2026-07-31 · max_drift 0.0 · 0 unpositioned · 0 unterritoried
windows 0 bad · cards 0 bad · datekinds 0 bad · eras 0 bad · forecast 0 bad
```

**GATE: PASS (baselines hold).**

```
DRIFT vs last nightly:
  ↑ events.explicit_pct        91.3  → 91.4
  ↓ events.target_pct          81.6  → 81.4   ERODED
  ↑ review.pct                 9.791 → 9.633
  ↑ forecast.grounded_direct   1434  → 1437
RATCHET: review.pct 9.791 → 9.633 · forecast.grounded_direct 1434 → 1437
```

`target_pct` is flagged ERODED and the gate still passed, correctly: the drift block compares
against the last run and prints any backward step, while the gate compares against the frozen
baseline (81.6) with a 1.0pp tolerance (`audit_all.py:273`). A 0.2pp step is inside it. That is
the drift block doing its job — printing decay that the tolerance would otherwise absorb in
silence. Two metrics improved and ratcheted, so that slack is spent and cannot be handed back.

**K and R still have zero direct grounding pages** and carry the maximum 1.25 widening. They are
the two axes r5 added on 2026-08-17; nothing has been written into the wiki that grounds them.

---

## 3. Forecast update

```
fresh 25 · incidents 23 (2 reports collapsed) · applied 3 · residue 20 · contested 0
recovered late 20 (event dates 2026-08-14 … 2026-08-18) · seen ledger 331
schema additions 0 — not a Monday; the review last added anything 2026-08-10
```

**No SCHEMA ADDITIONS block this run.**

### The three, with their drivers

| rule | tier | ×impact | driver |
|---|---|---|---|
| `ev-safety-research` | minor | ×0.00057 (k=9.61, spread 1.00, 1 report / 2 src) | OpenAI published an update on its August 7 determination that Astra may reach a critical cyber threshold |
| `ev-capital-commitment` | minor | ×0.00040 (k=14.73, spread 0.71, 2 reports / 2 src) | OpenAI told investors, in figures reported 2026-08-18, that revenue grew 18% quarter over quarter |
| `ev-capital-commitment` | minor | ×0.00032 (k=14.73, spread 0.71, 1 report / 1 src) | Etched said on 2026-08-18 it raised $700M at a $21B valuation |

### Day-over-day marginal movement (08-18 → 08-19, pp)

```
A   A1 -0.0412  A2 -0.0087  A3 +0.0706  A4 -0.0064  A5 -0.0038  A6 -0.0019  A7 -0.0087
E   E1 +0.0803  E2 -0.0084  E3 -0.0592  E4 -0.0088  E5 -0.0039
D   D1 +0.0016  D2 -0.0096  D3 +0.0008  D4 +0.0072      (grounding only — no evidence applied)
T · K · C · R · S · P   — no movement at all
```

Mainline unchanged: **T2 · K1 · A2 · C1 · R4 · D2 · S1 · P3 · E3.**

**All three applications point the right way tonight.** `ev-safety-research` moved weight from A1
"Control lost undetected" toward A3 "Catch buys pause", and a lab publishing its own critical-
threshold determination *is* a catch. The capital rules moved E1 "Boom holds" up and E3
"Valuation correction" down on rising revenue and a funded raise. Worth saying without softening
it: the direction is a fixed property of each rule, not a reading of the finding — it lands right
tonight and has landed wrong before. Tonight is not evidence the mechanism improved.

---

## 4. Panels still keyed to pre-r5 letters — re-verified tonight against `r6-2026-08-18`

The literals in `Research/timelines/forecast_emit.py:41-72` are still the 2026-07-31 originals.
They were written against r4's meanings and have never been re-keyed. Checked one by one against
the live registry:

**Crisis board — 3 of 6 wrong, 3 hold.**

| card | keyed to | r6 meaning | verdict |
|---|---|---|---|
| No superintelligence this window (0.18) | `T4` | "2037 to 2050" — an **arrival** window | **inverted.** The no-arrival cell is `T5` "Method asymptote", at 0.12 |
| Explosive tempo (**SC** by 2028) (0.10) | `T1` | "2027 to 2028" | **wrong quantity.** T now measures *"the year frontier systems first run the AI research loop end to end"* — the **researcher** line, not the superhuman-coder line |
| US-China **transparency** deal by 2032 (0.138) | `C3` | "Declaratory accord" — a declaratory *text* | **drifted.** C's ladder puts verification at `C5` "Verified limit holds" |
| Alignment fails undetected (0.089) | `A1` | "Control lost undetected" | holds |
| The bubble deflates hard (0.321) | `E3` | "Valuation correction" | holds |
| Superhuman AI researcher by 2035 (0.678) | band: year 2035, level 4.0 | — | **holds, and is now the best-aligned card on the board** |

That last row is the whole lesson again: the only card keyed to a **measured quantity** rather
than a position letter came through two rebuilds not merely intact but *more* correct, because T
was redefined to measure exactly what it already asked. Every card keyed to a letter silently
inherited whatever that letter came to mean.

**Claims register — 3 of 4 mis-keyed.**

- `cl-sc-2027` "superhuman-coder milestone crossed" → `T1`. Same wrong quantity as the crisis
  card: T1 is the researcher line under r6.
- `cl-correction-2027` "major AI **capex** correction" → `E2`/`E3` (margin squeeze, valuation
  correction). The capex cell is `E4` "Capital expenditure cut".
- `cl-state-laws-2026` "state AI-law count exceeds 90" → `C4` (US–CN domain-confined limit). Home
  is `R2` "Contested patchwork" — whose own axis description is where the 109-law figure lives.
- `cl-deal-2029` "US-CN AI agreement concluded" → `C3` "Declaratory accord". Holds, loosely: an
  accord concluded is an agreement concluded.

Also still carrying r4 language: `engine.json`'s milestone explainer says *"the T4 fraction never
arrives"*, and `why_shape` narrates a *"{t4}% no-SC floor"*. Both now name the wrong band. The
templates `quiet-decades` and `long-stagnation` both `req: T4` for outcomes that describe
non-arrival.

---

## 5. New tonight

**`marginals.history` is neither deduplicated nor date-ordered.** 26 entries over **20 distinct
dates**. Stored order ends: `… 08-17, 08-16, 08-17, 08-17, 08-18, 08-17, 08-18, 08-19`.
`2026-08-17` is stored **four times**; 08-13, 08-16 and 08-18 twice each. Any consumer reading
`history[-2]` as "yesterday" is correct tonight only by luck, and would have been wrong on 08-16
and 08-17. Any window drawn by **row count** over this array weights some days 2–4×. Today's
movement in §3 was computed by taking the last entry **per distinct date**, not by array
position.

**The collapser missed a same-night duplicate.** Two fingerprints for the identical Pew finding —
`d979c1e3aa250476` "Pew Research Center reported on August 18, 2026 that 55% of U.S. adults under
30 **are** more concerned…" and `a7bae859cb050d9a` "**The** Pew Research Center reported on August
18, 2026 that 55% of U.S. adults under 30 **were** more concerned…" — same date, same section,
`reports=1` each. A leading article and one verb tense split them. Both fell through to residue.
Tonight's 2 collapses were the OpenAI investor figures and the NSF commitment.

**The drift header again labelled itself with today's date** (second consecutive night). At gate
time `baselines.json` already held `last_run.date = 2026-08-19` while its stored *checks* were
08-18's. I verified those stored values against the 08-18 nightly commit — `91.3 / 81.6 / 9.791 /
1434`, identical — so **no day-over-day information was lost** and the comparison in §2 is a true
night-over-night one. The mechanism is the exposure: `audit_all.py:397` stamps `last_run` with
`TODAY` regardless of which staged data the run actually read, so any audit run earlier in the day
re-dates the record. It was harmless both nights because those runs preceded the emit and
reproduced the previous numbers. A run landing *after* a partial emit would make the drift block
compare tonight against tonight and print nothing.

**The residue is a coverage map, and it is mostly blank space.** 20 of 23 incidents went
unexplained — 87%. The axes that moved tonight were A and E (plus D by grounding alone); **T, K,
C, R, S and P did not move at all.** Sitting in tonight's residue, unmatched by any rule:

- a Pennsylvania executive order on data-center siting (2026-08-18) → `S3` "Power and siting bind"
- **both** Pew readings on under-30 concern, and New York's Futures listening sessions → `P`
- OpenAI rewriting its Preparedness Framework, and Anthropic's 186-page risk report → `A`/`R`
- NSF's $1.5B commitment, Cerebras' CS-4, Marvell's warrant to Google, Unitree closing its STAR
  Market debut 460% above IPO → `S`/`E`
- four state AGs' opening arguments against Meta → `R`

`P` and `S` each had multiple developments in today's record and no rule that takes them as
subject. This is the same finding as 2026-08-16 — the rule library, not the world, decides which
axes can hear anything — and tonight it is visible as a direct list.

---

## 6. What I did not do

- **No fixes.** This is a report run; the gate passed and nothing required an override. `SKIP_AUDIT`
  was not used and was not needed.
- **No queue task filed.** Every defect above is in the Atlas's own code — the crisis/claims
  literals, the history array, the collapser's fingerprint, the `last_run` stamp. None is a wiki
  data defect, so none belongs in `Wiki/_meta/queue/`. The duplicate Pew pair is two sources
  reporting one finding, which is normal and is exactly what the collapser is supposed to absorb.
- **The 109-law figure is not resolved**, only recorded. Settling `cl-state-laws-2026` needs NCSL
  or a named equivalent ingested as a witness, as the witness itself already states.
